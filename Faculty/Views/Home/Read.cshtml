
@{
    ViewBag.Title = ViewBag.Name;
}
<br /><br /><br />
<h2 class="head-pos">@ViewBag.Name</h2>
<div class="container">
    <p class="text">@ViewBag.Content</p>
    <div class="course-info">
        <span><b>Количество студентов:</b> @ViewBag.CountStud</span>
        <span><b>Преподаватель:</b> @ViewBag.Teacher</span>
        <span><b>Начало курса:</b> @ViewBag.StartDate</span>
        <span><b>Завершение курса:</b> @ViewBag.FinishDate</span>
        @if (ViewBag.CanRegister)
        {
            using (Html.BeginForm("Registration", "Home", FormMethod.Post))
            {
                <input type="hidden" value="@ViewBag.Name" name="Name" />
                <input type="submit" value="Зарегистрироваться" class="btn btn-primary" />
            }
        }

    </div>
</div>

